# 두두택배 접수 시스템 - 개발 지침 및 내 규칙

## 프로젝트 개요
- 두두택배 레거시 원장(shipments.csv) 조사, 정제(shipments_re.csv) 및 접수 시스템 구축 프로젝트

## 내 규칙 (Data Processing & Business Rules)

### 1. 요금 자동 산정 규칙
- **조사 발견점**: 요금이 누락(215건)되거나 부피 무게(가로x세로x높이/6000)를 반영하지 않아 요율이 손실된 건이 275건 존재.
- **방침**: 실측 무게와 부피 무게 중 큰 값(`billed_weight_kg`)으로 크기 등급(`size_grade`) 및 도착 권역(`region_type`) 기준 요금을 자동 산정.
- **구현 기능**: 가로, 세로, 높이, 무게 입력 시 실시간 부피무게 및 요금 자동 계산 기능.

### 2. 도착 지역 입력 제한
- **조사 발견점**: `receiver_area`에 'SEOUL', '서울시', '서울특별시' 등 25가지 유동적 입력이 혼재됨.
- **방침**: 자유 텍스트 입력을 차단하고 18개 표준 시/도 표준 목록 선택만 허용.
- **구현 기능**: 도착 지역 셀렉트 드롭다운 컴포넌트.

### 3. 접수 규격 제한 및 Validation
- **조사 발견점**: 무게 0kg 이하, 세 변의 합 160cm 초과, 25kg 초과 건이 접수되어 있음.
- **방침**: 상한 초과 건 및 비정상 수치 입력 시 저장을 막고 경고 표기.
- **구현 기능**: 입력 폼 유효성 검사 (규격 초과 시 버튼 비활성화 및 Alert).

### 4. 상태 및 시각 동기화
- **조사 발견점**: `status`가 배송완료이나 `delivered_at`이 누락(322건)되거나 완료시각이 접수시각보다 빠른 오류(40건).
- **방침**: 배송 추적 이벤트(`tracking_events.csv`)의 최종 시각을 기준으로 완료 시각을 보정하며, 시점역전(40건) 및 시각어긋남(70건)은 식별 마크를 부여.
- **구현 기능**: 트래킹 이벤트 연동 배송 완료 시각 자동 채움.

### 5. 금지 품목 검증 및 이력 관리
- **조사 발견점**: 규정 상 금지 품목(현금, 라이터, 부탄가스, 보조배터리 등)이 물품명에 54건 입력된 사례 발견.
- **방침**: 신규 접수 시 실시간 검수로 차단하며, 레거시 데이터는 `[금지품목]` 태깅 처리하여 분류.
- **구현 기능**: 접수 시 금지 품목 텍스트 검수 필터 및 태그 분류.

## 보류 및 예외 항목 분류 조치 (Completed Refinements)
- **수취인 성명 호칭 정제 (49건)**: 성명 뒤에 붙은 ` 님` 호칭 삭제 정제 완료.
- **전화번호 앞 0 누락/00 중복 보정 (51건)**: `10XXXXXXXX` $\rightarrow$ `010-XXXX-XXXX` 등 표준 휴대폰 포맷으로 보정 패치 완료.
- **중복 운송장 번호 (45건)**: 2번째 이상 등장 시 `-DUP` 접미사 부여로 PK 고유성 100% 확보.
- **전화번호 누락 (265건)**: `000-0000-0000` 식별 플레이스홀더 할당하여 분류 완료.
- **이상 마크 태깅**: `[시점역전]` 40건, `[시각어긋남]` 70건, `[치수이상]` 41건, `[금지품목]` 54건 마킹 조치 완료.
- **고객 명부 미등록 sender_id (56건)**: 원형 수용 유지 (무시).
