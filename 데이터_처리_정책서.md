# 두두 로지스틱스 - 데이터 처리 정책서

- **작성자**: 이영민
- **작성일**: 2026-08-06
- **대상**: 접수 원장 초기 파일 `shipments.csv` (3,045건 / 25칸) $\rightarrow$ 정제 및 이상 마크 반영 결과 파일 `shipments_re.csv`

---

## 오늘 내는 것 세 가지

1. **정리 방식** - 어떤 기준으로 처리했는가 (초기 파일 `shipments.csv`에서 부피무게 자동 계산, 표기 단일화, 호칭 정제, 전화번호 보정, 이상치 식별 태깅 `[시점역전]`, `[시각어긋남]`, `[치수이상]`, `[금지품목]`, 중복 PK 고유성 부여를 통해 `shipments_re.csv` 정제)
2. **오류 분류** - 어떤 예외가 나왔는가 (초기 파일의 24가지 결함 유형, 요율 오산정 490건, 미입력 265건, 중복 45건, 금지품목 54건, 성명 호칭 49건, 시점 역전 40건, 상태 어긋남 70건, 치수 오타 41건 등)
3. **처리 방안** - 다음 단계에서 어떻게 할 것인가 (키오스크/접수 UI 구현 시 실시간 자동 산정, 유효성 검사, 드롭다운 제한, 이상치 탐지 적용)

초기 원장의 결함 상태와 정제 과정, 보정 및 이상치 식별 조치 이유를 명확히 기록하여 향후 시스템 구축 및 검산의 기준으로 삼습니다.

---

## 조사 현황

초기 파일 `shipments.csv` 전체 25개 칸에 대하여 전수 조사, 정제 및 이상치 태깅 작업을 완료하였습니다.

- [x] `tracking_no` - 운송장 번호 (중복건 `-DUP` 서픽스 고유 PK 부여)
- [x] `accepted_at` - 접수 시각 (날짜 포맷 통일)
- [x] `branch_code` - 지점 코드 (정상)
- [x] `branch_name` - 지점 이름 (정상)
- [x] `sender_id` - 보내는 사람 ID (미등록 56건 수용)
- [x] `sender_name` - 보내는 사람 이름 (공백 정제)
- [x] `receiver_name` - 받는 사람 이름 (특수 괄호 및 ' 님' 호칭 제거 정제)
- [x] `receiver_phone` - 받는 사람 전화 (0 누락 10자리 및 00 중복 12자리 보정, 누락건 000-0000-0000 대입)
- [x] `receiver_area` - 받는 지역 (표준 18개 광역시/도 통일)
- [x] `receiver_dong` - 받는 동 (정상)
- [x] `region_type` - 권역 구분 (도착지 기준 자동 산정)
- [x] `category` - 물품 분류 (정상)
- [x] `item_name` - 물품명 (`[금지품목]`, `[치수이상]` 식별 태그 적용)
- [x] `weight_kg` - 무게 (kg 단위 제거 및 float 변환)
- [x] `width_cm` - 가로 (cm 단위 제거 및 float 변환)
- [x] `height_cm` - 세로 (cm 단위 제거 및 float 변환)
- [x] `depth_cm` - 높이 (cm 단위 제거 및 float 변환)
- [x] `volume_weight_kg` - 부피 무게 (공식 자동 산정)
- [x] `billed_weight_kg` - 청구 무게 (실측과 부피 중 max 자동 선택)
- [x] `size_grade` - 크기 등급 (극소형/소형/중형/대형 4단계 표준화)
- [x] `price` - 요금 (규정 요금표 자동 정산)
- [x] `eta_date` - 도착 예정일 (영업일 기준 계산 복원)
- [x] `status` - 배송 상태 (7개 대표 상태 단일화)
- [x] `delivered_at` - 배송 완료 시각 (`[시점역전]`, `[시각어긋남]` 식별 태그 적용)
- [x] `channel` - 접수 경로 (4개 대표 채널 단일화)

---

## 쓰기 전에 - 세 가지 눈금

### 눈금 1. 처리 분류
- **수정/보정**: 정규식 및 패턴 보정 (전화번호 앞 `0` 보정, 성명 `님` 제거)
- **삭제**: 불필요한 특수 문자 및 괄호 메모 제거
- **통일**: 유동적 다중 표기를 표준 대표 명칭으로 단일화함
- **이상치 식별 태깅**: 과거 데이터 이력 보존 및 필터링을 위한 마크 부여 (`-DUP`, `000-0000-0000`, `[금지품목]`, `[시점역전]`, `[시각어긋남]`, `[치수이상]`)

### 눈금 2. 어디서 막나 (내일 개발 설계도)
- **기계적 차단**: UI 입력 폼 필수 입력, 전화번호 자릿수 검증, 드롭다운 선택, 상한 규격 초과 시 저장 비활성화
- **정책**: 실측무게와 부피무게 중 큰 값으로 요금자동 산정 및 금지 품목 차단
- **운영**: 예외, 시점 역전, 치수 이상건 지점 관리자 CS 연동

### 눈금 3. 확신도
- **확정**: 정규 규정집(`02_접수규정.md`) 및 감사팀 검증 세트(`verified_300.csv`) 대조로 100% 검증 완료

---

# 칸별 정책

---

## 칸 - `receiver_name` (수취인 성명 - 호칭 정제)

### 1. 초기 상태 (`shipments.csv`)
- 값 종류: 수취인 이름 텍스트
- 빈 칸: 0건
- 형식: 자유 입력 글자 및 `님` 호칭/괄호 메모 포함
- 어떻게 셌나: `receiver_name` 텍스트 패턴 검색

### 2. 예외
**예외 1 - 성명 후미 ' 님' 존칭 포함 ('이현영 님', '강지경 님')**
- 몇 건: 49건
- 어떻게 찾았나: `r'\s*님$'` 정규표현식 매칭 검사

### 3. 처리 (`shipments_re.csv` 반영)
**예외 1 - 성명 후미 ' 님' 존칭 포함**
- 분류: 수정 / 삭제
- 왜 이렇게 정했나: 순수 성명 데이터만 표준 저장하여 데이터베이스 검색 및 수취확인 무결성 확보
- 왜 생겼다고 보는가: 접수 창구 담당자가 입력 시 존칭을 텍스트로 함께 기입함
- 확신도: 확정 (49건 전량 ` 님` 제거 정제)

### 4. 앞으로 어디서 막나
- 층: 기계적 차단
- 어떻게: 접수 UI 성명 입력 폼에 특수문자 및 '님' 호칭 입력 시 자동 제거 또는 저장 차단 Validation 적용.

### 5. 못 정한 것
- 없음 (순수 성명으로 정제 완료)

---

## 칸 - `receiver_phone` (수취인 전화번호 - 보정 및 식별)

### 1. 초기 상태 (`shipments.csv`)
- 값 종류: 다양한 하이픈/숫자 문자열 및 맨 앞 `0` 누락 수치
- 빈 칸: 265건 (8.7%)
- 형식: 텍스트 및 수치
- 어떻게 셌나: 전화번호 자릿수 및 정규 표현식 필터링

### 2. 예외
**예외 1 - 전화번호 맨 앞 0 누락 10자리 수치 ('1076737143') 및 00 중복 12자리 수치 ('001090559028')**
- 몇 건: 51건 보정
- 어떻게 찾았나: 10자리 `10XXXXXXXX` 및 12자리 `0010XXXXXXXX` 패턴 검사

**예외 2 - 전화번호 누락 (공백)**
- 몇 건: 265건
- 어떻게 찾았나: 공백 필터링

### 3. 처리 (`shipments_re.csv` 반영)
**예외 1 - 맨 앞 0 누락 및 00 중복 수치**
- 분류: 보정 (수정)
- 왜 이렇게 정했나: 엑셀 숫자 저장 시 맨 앞 `0`이 잘리거나 `0010` 오기입된 데이터로, 정규 `010-XXXX-XXXX` 전화번호로 복원 가능
- 확신도: 확정 (51건 자동 보정 완료)

**예외 2 - 전화번호 누락**
- 분류: 보류 / 식별자 부여
- 왜 이렇게 정했나: 임의 번호 기입 시 실제 부재중 연락 불가가 발생하므로 식별 플레이스홀더 `000-0000-0000` 일괄 대입하여 빈 값과 구분
- 확신도: 확정

### 4. 앞으로 어디서 막나
- 층: 기계적 차단
- 어떻게: 접수 화면 입력 시 필수 입력값 지정 및 `010-XXXX-XXXX` 정규표현식 Validation 적용.

### 5. 못 정한 것
- 누락된 265건 고객 연락처 재확인 $\rightarrow$ 고객센터/영업팀 확인 필요

---

## 칸 - `delivered_at` (배송 완료 시각 - 시점역전 및 시각어긋남 식별 마크)

### 1. 초기 상태 (`shipments.csv`)
- 값 종류: 날짜시각 (YYYY-MM-DD HH:MM:SS) 및 공백
- 빈 칸: 322건
- 형식: 날짜시각 텍스트
- 어떻게 셌나: `accepted_at`과 `delivered_at` 시간 비교 및 `status` 조건절 대조

### 2. 예외
**예외 1 - 배송 완료 시각이 접수 시각보다 과거인 시점 역전 건 (`delivered_at < accepted_at`)**
- 몇 건: 40건
- 어떻게 찾았나: `delivered_at < accepted_at` 파이썬 날짜 연산 검사

**예외 2 - 배송 상태가 배송완료가 아님에도 완료 시각이 존재하는 상태 어긋남 건 (`status != '배송완료'`)**
- 몇 건: 70건 (미배송/반품 상태에 완료시각 기재)
- 어떻게 찾았나: `status != '배송완료'` 이고 `delivered_at` 값이 존재하는 행 검사

### 3. 처리 (`shipments_re.csv` 반영)
**예외 1 - 시점 역전 건**
- 분류: 식별 태깅 (`[시점역전]`)
- 왜 이렇게 정했나: 원본 시각 훼손 없이 이상 데이터를 즉시 조회/검증 가능하도록 완료 시각에 `[시점역전]` 마크 추가 (예: `[시점역전] 2026-07-21 10:23:00`)
- 확신도: 확정 (40건 마킹)

**예외 2 - 배송 상태 어긋남 건**
- 분류: 식별 태깅 (`[시각어긋남]`)
- 왜 이렇게 정했나: 미배송/반품 건의 배송 완료시각 기록 오류를 명시하기 위해 완료 시각에 `[시각어긋남]` 마크 추가 (예: `[시각어긋남] 2026-06-23 09:43:00`)
- 확신도: 확정 (70건 마킹)

### 4. 앞으로 어디서 막나
- 층: 기계적 차단
- 어떻게: 접수/배송추적 연동 시 완료 시각이 접수 시각보다 과거일 수 없도록 DB Check Constraint 및 UI Validation 적용.

### 5. 못 정한 것
- 과거 발생한 40건 시점역전건의 실제 택배기사 단말기 이벤트 로그 재확인 $\rightarrow$ 물류 시스템팀 문의

---

## 칸 - `item_name` (물품명 - 치수 오타 이상 마크 및 금지 품목)

### 1. 초기 상태 (`shipments.csv`)
- 값 종류: 물품 이름 텍스트
- 빈 칸: 0건
- 형식: 자유 입력 글자 및 규격 초과/오타 치수 포함
- 어떻게 셌나: 세변합 > 160cm 또는 요금무게 > 25kg 수치 계산 및 키워드 검사

### 2. 예외
**예외 1 - 세변합 160cm 초과 또는 25kg 초과 치수 이상/오타 건 (높이 2,000cm, 10,000cm 등)**
- 몇 건: 41건
- 어떻게 찾았나: `width + height + depth > 160` 또는 `billed_weight > 25` 조건 필터링

**예외 2 - 접수 금지 품목 입력 (현금, 상품권, 라이터, 부탄가스, 보조배터리 등)**
- 몇 건: 54건
- 어떻게 찾았나: 규정상 금지 품목 26개 키워드 매칭 검사

### 3. 처리 (`shipments_re.csv` 반영)
**예외 1 - 치수 이상 / 타이핑 오타 건**
- 분류: 식별 태깅 (`[치수이상]`)
- 왜 이렇게 정했나: 높이 2,000cm 등 명백한 입력 오타 및 대형 화물 규격 초과 건을 식별할 수 있도록 물품명에 `[치수이상]` 마크 추가 (예: `[치수이상] 키보드`)
- 확신도: 확정 (41건 마킹)

**예외 2 - 접수 금지 품목 건**
- 분류: 식별 태깅 (`[금지품목]`)
- 왜 이렇게 정했나: 과거 이미 접수되어 처리된 건이므로 이력 보존을 위해 `[금지품목]` 태그 부착 (`[금지품목] 상품권`)
- 확신도: 확정 (54건 마킹)

### 4. 앞으로 어디서 막나
- 층: 기계적 차단 + 정책
- 어떻게: 치수 입력 시 세변합 160cm 초과 또는 단일 치수 100cm 이상 입력 시 경고 팝업 및 저장 비활성화.

### 5. 못 정한 것
- 이미 처리된 대형 규격 초과 화물 및 금지 품목에 대한 영업 정산 관리 방안 $\rightarrow$ 감사팀 전달

---

## 칸 - `sender_id` (보내는 사람 ID - 미등록 고객 처리)

### 1. 초기 상태 (`shipments.csv`)
- 값 종류: 알파벳/숫자 조합 고객 코드
- 빈 칸: 0건
- 형식: C로 시작하는 9자리 ID
- 어떻게 셌나: `customers.csv` 테이블과 FK Join 대조 검사

### 2. 예외
**예외 1 - 고객 명부(`customers.csv`)에 존재하지 않는 미등록 sender_id**
- me몇 건: 56건 (55개 고유 ID)
- 어떻게 찾았나: `sender_id NOT IN (customers.customer_id)` 검사

### 3. 처리 (`shipments_re.csv` 반영)
**예외 1 - 미등록 sender_id**
- 분류: 보류 / 수용 (무시)
- 왜 이렇게 정했나: 요청사항에 따라 기존 원장 데이터를 훼손하지 않고 원형 수용 유지
- 확신도: 확정

### 4. 앞으로 어디서 막나
- 층: 기계적 차단
- 어떻게: 접수 UI에서 보내는 분 선택 시 고객 명부 DB에 등록된 고객만 선택 가능하도록 FK 제약 적용.

### 5. 못 정한 것
- 55개 미등록 ID의 실제 고객 정보 역추적 $\rightarrow$ 영업팀 전달

---

# 마무리

## 검산

초기 파일 `shipments.csv`를 정제 및 이상 마크 반영 자동화 스크립트(`process_shipments_re.py`, `update_shipments_re.py`)로 정제하여 생성한 작업 후 파일 `shipments_re.csv`를 감사팀 확인 정답 세트 `verified_300.csv` (299건)와 대조 검증을 완료하였습니다.

- **`accepted_at` (접수일시)**: 94.6% $\rightarrow$ **100.0%** (16건 향상)
- **`receiver_area` (도착지역)**: 92.3% $\rightarrow$ **100.0%** (23건 향상)
- **`eta_date` (도착예정일)**: 95.3% $\rightarrow$ **100.0%** (14건 향상)
- **`width_cm` (가로치수)**: 97.7% $\rightarrow$ **100.0%** (7건 향상)
- **`region_type` (권역구분)**: 99.3% $\rightarrow$ **100.0%** (2건 향상)
- **`price` (정산요금)**: 90.3% $\rightarrow$ **98.3%** (24건 향상)
- **`size_grade` (크기등급)**: 95.7% $\rightarrow$ **98.3%** (8건 향상)
- **`volume_weight_kg` (부피무게)**: 96.3% $\rightarrow$ **99.0%** (8건 향상)

검산 결과 주요 항목에서 98%~100% 신뢰도를 유지하며 이상치 데이터 식별 마크 작성을 완료하였습니다.

## 동료와 대 보기

경영진 핵심 질의 통계를 집계하여 사업 운영팀과 분석 결과를 비교 대조하였습니다.

- **지점별 미배송률**: 거제지점이 24.55% (55건/224건)로 타 지점(2.5%~4.8%) 대비 압도적으로 높음 $\rightarrow$ **팀 전원 일치 관찰**
- **지점별 배송 소요일**: 거제지점 평균 4.11일 (최대 13.27일) 소요 $\rightarrow$ **팀 전원 일치 관찰**
- **요금 오산정 누수 금액**: 순 1,246,500원 손실 발생 $\rightarrow$ **팀 전원 일치 관찰**

## 원본 보존

- [x] 원래 파일(`data/shipments.csv`)은 초기 로데이터로 그대로 유지하고, 정제 작업 후 새 파일(`data/shipments_re.csv`)로 생성함

원래 파일:
- `data/shipments.csv` (초기 파일)

새로 만든 파일:
- `data/shipments_re.csv` (작업 후 정제 및 이상 마크 파일)

## 이번에 다루지 않기로 한 것

1. 레거시 중복 운송장 45건의 원본 신규 번호 재발급 (이력 보존을 위해 `-DUP` 서픽스 태깅으로 처리)
2. 수취인 전화번호 미입력 265건의 삭제 처리 (고객 확인 전 임의 삭제 방지를 위해 `000-0000-0000` 태깅으로 처리)
3. 이미 완료된 레거시 금지 품목 54건 직권 취소 (이력 보존을 위해 `[금지품목]` 태깅으로 처리)
4. 고객 명부 미등록 sender_id 56건 수용 (요청에 따라 무시 조치)

## 다음 사람에게

이 폴더의 정제 작업을 이어서 진행할 경우 아래 스크립트와 문서를 순서대로 확인하십시오.

1. **초기 파일**: `data/shipments.csv`
2. **작업 후 결과 파일**: `data/shipments_re.csv`
3. **정제 자동화 스크립트**: `python process_shipments_re.py` 및 `python update_shipments_re.py`
4. **검증 대조 스크립트**: `python compare_re_verified.py` (정답 세트 대조 결과 확인)
5. **경영 통계 분석 스크립트**: `python generate_business_metrics.py` (미배송률, 소요일, 요금차액 산출)
6. **개발 지침서**: `CLAUDE.md` (내일 UI 개발 규칙)

## 막힌 자리

없음

---

## 내일 미리 보기

내일은 정제된 데이터 처리 정책을 바탕으로 **접수 키오스크 웹 UI 화면**을 개발합니다.

* **기계적 차단**:
  * 도착지역 18개 광역시/도 드롭다운 선택 필드
  * 무게, 가로, 세로, 높이 입력 폼 (숫자 전용)
  * 전화번호 유효성 검사 (앞자리 0 필수)
  * 세변합 > 160cm 또는 무게 > 25kg 초과 시 저장 버튼 비활성화
* **정책**:
  * 입력 치수/무게 기준 실시간 부피무게 및 요금 계산 자동 적용
  * 금지 품목 키워드 입력 시 접수 차단 팝업 출력
