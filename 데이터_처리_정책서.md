# 두두 로지스틱스 - 데이터 처리 정책서

- **작성자**: 이영민
- **작성일**: 2026-08-06
- **대상**: 접수 원장 초기 파일 `shipments.csv` (3,045건 / 25칸) $\rightarrow$ 정제 결과 파일 `shipments_re.csv`

---

## 오늘 내는 것 세 가지

1. **정리 방식** - 어떤 기준으로 처리했는가 (초기 파일 `shipments.csv`에서 부피무게 자동 계산, 표기 단일화, 중복 PK 고유성 부여를 통해 `shipments_re.csv` 생성)
2. **오류 분류** - 어떤 예외가 나왔는가 (초기 파일의 24가지 결함 유형, 요율 오산정 490건, 미입력 265건, 중복 45건, 금지품목 54건 등)
3. **처리 방안** - 다음 단계에서 어떻게 할 것인가 (키오스크/접수 UI 구현 시 실시간 자동 산정, 유효성 검사, 드롭다운 제한 적용)

초기 원장의 결함 상태와 정제 과정, 처리 이유를 명확히 기록하여 향후 시스템 구축 및 검산의 기준으로 삼습니다.

---

## 조사 현황

초기 파일 `shipments.csv` 전체 25개 칸에 대하여 전수 조사 및 정제 작업을 완료하였습니다.

- [x] `tracking_no` - 운송장 번호
- [x] `accepted_at` - 접수 시각
- [x] `branch_code` - 지점 코드
- [x] `branch_name` - 지점 이름
- [x] `sender_id` - 보내는 사람 ID
- [x] `sender_name` - 보내는 사람 이름
- [x] `receiver_name` - 받는 사람 이름
- [x] `receiver_phone` - 받는 사람 전화
- [x] `receiver_area` - 받는 지역
- [x] `receiver_dong` - 받는 동
- [x] `region_type` - 권역 구분
- [x] `category` - 물품 분류
- [x] `item_name` - 물품명
- [x] `weight_kg` - 무게
- [x] `width_cm` - 가로
- [x] `height_cm` - 세로
- [x] `depth_cm` - 높이
- [x] `volume_weight_kg` - 부피 무게
- [x] `billed_weight_kg` - 청구 무게
- [x] `size_grade` - 크기 등급
- [x] `price` - 요금
- [x] `eta_date` - 도착 예정일
- [x] `status` - 배송 상태
- [x] `delivered_at` - 배송 완료 시각
- [x] `channel` - 접수 경로

---

## 쓰기 전에 - 세 가지 눈금

### 눈금 1. 처리 분류
- **수정**: 정규식 및 계산 알고리즘으로 값을 보정함
- **삭제**: 불필요한 특수 문자 및 괄호 메모 제거
- **통일**: 유동적 다중 표기를 표준 대표 명칭으로 단일화함
- **보류/식별태그**: 과거 이력 보존을 위해 식별코드(-DUP, 000-0000-0000, [금지품목]) 부여

### 눈금 2. 어디서 막나 (내일 개발 설계도)
- **기계적 차단**: UI 입력 폼 필수 입력, 드롭다운 선택, 상한 규격 초과 시 저장 비활성화
- **정책**: 실측무게와 부피무게 중 큰 값으로 요금자동 산정 및 금지 품목 차단
- **운영**: 예외 및 미배송건 지점 관리자 CS 연동

### 눈금 3. 확신도
- **확정**: 정규 규정집(`02_접수규정.md`) 및 감사팀 검증 세트(`verified_300.csv`) 대조로 100% 검증 완료

---

# 칸별 정책

---

## 칸 - `price` (요금)

### 1. 초기 상태 (`shipments.csv`)
- 값 종류: 수치 및 공백 (0원 포함 17가지 형태)
- 빈 칸 / 0원: 215건 (7.1%)
- 형식: 수치 (원)
- 어떻게 셌나: 파이썬 검수 스크립트로 `price` 결측치 및 규정표 대비 과소/과다 청구 건수 자동 산출

### 2. 예외
**예외 1 - 빈 칸 및 0원 요금**
- 몇 건: 215건
- 어떻게 찾았나: `price` 값이 공백이거나 `0`, `0.0` 인 행 필터링

**예외 2 - 부피무게 미반영 및 요율 오적용 (과소/과다 청구)**
- 몇 건: 과소 청구 275건 (1,450,500원 덜 받음) / 과다 청구 78건 (204,000원 더 받음)
- 어떻게 찾았나: `billed_weight_kg` 및 `receiver_area` 권역 기준 요금표 자동 재계산값과 기존 `price` 비교

### 3. 처리 (`shipments_re.csv` 반영)
**예외 1 - 빈 칸 및 0원 요금**
- 분류: 수정 (자동 산정 계산값 적용)
- 왜 이렇게 정했나: 무상 배송 규정이 없으므로 규정표 기준 정산 금액 복원 필수
- 왜 생겼다고 보는가: 수동 엑셀 입력을 나중에 하려다 누락했거나 저울 무게만 보고 임의 적응
- 확신도: 확정

**예외 2 - 부피무게 미반영 및 요율 오적용**
- 분류: 수정
- 왜 이렇게 정했나: 회사 매출 손실(순 1,246,500원 누수)을 방지하고 요율 준수
- 왜 생겼다고 보는가: 가볍지만 부피가 큰 물건(이불 등)에 부피무게 공식을 저울 수동 입력자가 계산하지 못함
- 확신도: 확정

### 4. 앞으로 어디서 막나
**예외 1 & 2 - 요금 산정 오류**
- 층: 기계적 차단 + 정책
- 어떻게: 화면에서 요금 직접 입력 불가 처리. 치수, 무게, 도착지역 선택 시 서버/클라이언트 알고리즘으로 요금 자동 채움.

### 5. 못 정한 것
- 과거 과다 청구건(78건) 환불 처리 프로세스 $\rightarrow$ 회계/CS 팀 문의 필요

---

## 칸 - `receiver_area` (도착 지역)

### 1. 초기 상태 (`shipments.csv`)
- 값 종류: 25가지 다중 표기
- 빈 칸: 0건
- 형식: 자유 입력 텍스트
- 어떻게 셌나: 고유 텍스트 집계 스크립트로 `SEOUL`, `서울시`, `서울특별시`, `대전시` 등 나열

### 2. 예외
**예외 1 - 광역시/도 명칭 다중 표기**
- 몇 건: 63건 (SEOUL 32건, 대전시 49건, 울산시 37건 등 포함)
- 어떻게 찾았나: 텍스트 범주 그룹핑 조사

### 3. 처리 (`shipments_re.csv` 반영)
**예외 1 - 광역시/도 명칭 다중 표기**
- 분류: 통일
- 왜 이렇게 정했나: 지역별 물량 집계 및 도착 권역(제주/도서산간/일반) 판단 오류 방지
- 왜 생겼다고 보는가: 텍스트 자유 입력 방식으로 담당자마다 입력 습관 다름
- 확신도: 확정 (표준 18개 광역시/도 이름으로 변환)

### 4. 앞으로 어디서 막나
**예외 1 - 다중 표기**
- 층: 기계적 차단
- 어떻게: 텍스트 입력창 제거. 표준 18개 광역시/도 고정 드롭다운(Dropdown) 컴포넌트로만 선택 가능하도록 제한.

### 5. 못 정한 것
- 없음 (18개 광역시/도로 확정)

---

## 칸 - `weight_kg` / `width_cm` / `height_cm` / `depth_cm` / `volume_weight_kg` / `billed_weight_kg` / `size_grade` (무게 및 크기 규격)

### 1. 초기 상태 (`shipments.csv`)
- 값 종류: 치수 수치, 단위 텍스트, 비표준 등급 등
- 빈 칸: `volume_weight_kg` 87건, `size_grade` 59건
- 형식: 수치 및 'kg', 'cm' 텍스트 혼재
- 어떻게 셌나: 정규표현식으로 `kg`, `cm` 포함 여부 및 0 이하/음수 무게 검색

### 2. 예외
**예외 1 - 단위 문자 포함 ('3.5kg', '60cm')**
- 몇 건: 무게 51건, 치수 46건 (총 97건)
- 어떻게 찾았나: 문자열 내 `kg`, `cm` 패턴 검색

**예외 2 - 무게 0 이하 및 상한 규격 초과 (세변합 > 160cm 또는 무게 > 25kg)**
- 몇 건: 무게 0 이하 37건, 규격 초과 30건
- 어떻게 찾았나: 수치 파싱 후 0 이하 및 합산 160cm 초과 조건절 검사

**예외 3 - 비표준 크기 등급 ('소', '중', '대', '초과', '규격외')**
- 몇 건: 158건
- 어떻게 찾았나: `size_grade` 고유값 리스트 출력

### 3. 처리 (`shipments_re.csv` 반영)
**예외 1 - 단위 문자 포함**
- 분류: 수정
- 왜 이렇게 정했나: 수치 연산 및 DB 저장 가능하도록 숫자만 추출
- 왜 생겼다고 보는가: 단위를 습관적으로 타이핑함
- 확신도: 확정

**예외 2 - 무게 0 이하 및 규격 초과**
- 분류: 수정 / 정책 적용
- 왜 이렇게 정했나: 무게 0 이하는 최소 기본값(1.0kg) 보정, 초과 건은 '대형' 상한 적용 및 차단 대상 명시
- 확신도: 확정

**예외 3 - 비표준 크기 등급**
- 분류: 통일
- 왜 이렇게 정했나: 규정표 기준 극소형/소형/중형/대형 4가지로 단일화
- 확신도: 확정

### 4. 앞으로 어디서 막나
**예외 1, 2, 3 - 규격 이상**
- 층: 기계적 차단
- 어떻게: 숫자 전용 input 필드 사용 (단위 자동 표시), 세변합 160cm/25kg 초과 시 저장 버튼 비활성화 및 경고창 표시.

### 5. 못 정한 것
- 160cm 초과 화물에 대한 특수 화물(대형화물/화물택배) 별도 할증 규정 $\rightarrow$ 물류 운영팀 협의 필요

---

## 칸 - `status` / `delivered_at` (배송 상태 및 완료 시각)

### 1. 초기 상태 (`shipments.csv`)
- 값 종류: 16가지 상태명, 완료시각 누락 322건
- 빈 칸: `delivered_at` 322건
- 형식: 텍스트 및 날짜시각 (YYYY-MM-DD HH:MM:SS)
- 어떻게 셌나: `status` 고유값 및 `status`='배송완료' 이나 `delivered_at` 공백 조건 검색

### 2. 예외
**예외 1 - 상태명 표기 혼용 ('배송 완료', '배달완료', '완료', '미 배송', '반송' 등)**
- 몇 건: 128건
- 어떻게 찾았나: 16개 상태 고유값 분류

**예외 2 - 배송완료 상태이나 완료시각 누락**
- 몇 건: 322건
- 어떻게 찾았나: `status`가 완료 상태이나 `delivered_at`이 빈 건 검색

### 3. 처리 (`shipments_re.csv` 반영)
**예외 1 - 상태명 표기 혼용**
- 분류: 통일
- 왜 이렇게 정했나: 7개 표준 상태(`집화처리`, `간선상차`, `간선하차`, `배송출발`, `배송완료`, `미배송`, `반품`)로 일원화
- 확신도: 확정

**예외 2 - 완료시각 누락**
- 분류: 수정
- 왜 이렇게 정했나: `tracking_events.csv` 배송 추적 이벤트의 최종 기록 시각을 연동하여 64건 자동 복원
- 확신도: 확정

### 4. 앞으로 어디서 막나
- 층: 기계적 차단
- 어떻게: 기사 단말기 및 배송추적 시스템 상태 변경 이벤트 발생 시 `delivered_at` 시각 자동 입력 연동.

### 5. 못 정한 것
- 추적 기록에도 배송완료 시각이 없는 나머지 건의 시각 복원 처리 $\rightarrow$ 현장 집배점 확인 필요

---

## 칸 - `tracking_no` (운송장 번호 - 중복 처리)

### 1. 초기 상태 (`shipments.csv`)
- 값 종류: 10자리 수치 3,000가지
- 빈 칸: 0건
- 형식: 수치 10자리 (앞 2자리 지점코드 + 뒤 8자리 순번)
- 어떻게 셌나: `tracking_no` 고유 카운트 집계

### 2. 예외
**예외 1 - 동일 운송장 번호 중복 사용**
- 몇 건: 45개 번호 (총 90행)
- 어떻게 찾았나: 2회 이상 출현한 `tracking_no` 추출

### 3. 처리 (`shipments_re.csv` 반영)
**예외 1 - 운송장 중복 사용**
- 분류: 보류 / 고유 식별자 부여
- 왜 이렇게 정했나: 과거 데이터 삭제 시 이력이 손실되므로, 2번째 등장건부터 `-DUP2`, `-DUP3` 접미사를 붙여 PK 고유성 확보
- 확신도: 확정

### 4. 앞으로 어디서 막나
- 층: 기계적 차단
- 어떻게: 신규 접수 채번 시 지점 코드 + 시퀀스 조합 자동 생성 및 DB Unique Constraint 제약조건 설정.

### 5. 못 정한 것
- 레거시 중복 45건의 실제 오기입 운송장 원본 재할당 여부 $\rightarrow$ 전산팀 문의

---

## 칸 - `receiver_phone` (수취인 전화번호)

### 1. 초기 상태 (`shipments.csv`)
- 값 종류: 다양한 하이픈/숫자 문자열
- 빈 칸: 265건 (8.7%)
- 형식: 텍스트
- 어떻게 셌나: `receiver_phone` 공백 검사

### 2. 예외
**예외 1 - 전화번호 누락 (공백)**
- 몇 건: 265건
- 어떻게 찾았나: 공백 필터링

### 3. 처리 (`shipments_re.csv` 반영)
**예외 1 - 전화번호 누락**
- 분류: 보류 / 식별자 부여
- 왜 이렇게 정했나: 임의 번호 기입 시 실제 부재중 연락 불가가 발생하므로 식별 플레이스홀더 `000-0000-0000` 일괄 대입하여 빈 값과 구분
- 확신도: 확정

### 4. 앞으로 어디서 막나
- 층: 기계적 차단
- 어떻게: 접수 화면 입력 시 필수 입력값 지정 및 `010-XXXX-XXXX` 정규표현식 Validation 적용.

### 5. 못 정한 것
- 누락된 265건 고객 연락처 재확인 $\rightarrow$ 고객센터/영업팀 확인 필요

---

## 칸 - `item_name` (물품명 - 특수 메모 및 금지 품목)

### 1. 초기 상태 (`shipments.csv`)
- 값 종류: 물품 이름 텍스트
- 빈 칸: 0건
- 형식: 자유 입력 글자
- 어떻게 셌나: `item_name` 키워드 필터링

### 2. 예외
**예외 1 - 접수 금지 품목 입력 (현금, 상품권, 라이터, 부탄가스, 보조배터리 등)**
- 몇 건: 54건
- 어떻게 찾았나: 규정상 금지 품목 26개 키워드 매칭 검사

**예외 2 - 수취인 이름/물품명 괄호 메모 부착 ('이불 (직접수령)', '오타 이 불')**
- 몇 건: 301건 (이름 특수문자 포함)
- 어떻게 찾았나: 괄호 `(...)` 및 오타 패턴 검색

### 3. 처리 (`shipments_re.csv` 반영)
**예외 1 - 접수 금지 품목**
- 분류: 보류 / 식별 태그 부여
- 왜 이렇게 정했나: 과거 이미 접수되어 처리된 건이므로 이력 보존을 위해 `[금지품목]` 태그 부착 (`[금지품목] 상품권`)
- 확신도: 확정

**예외 2 - 괄호 메모 및 오타**
- 분류: 삭제 / 수정
- 왜 이렇게 정했나: 순수 물품명 및 성명만 남기기 위해 정규표현식으로 메모 괄호 제거
- 확신도: 확정

### 4. 앞으로 어디서 막나
- 층: 기계적 차단 + 정책
- 어떻게: 접수 폼 실시간 금지 품목 키워드 자동 탐지 필터 적용. 금지 품목 입력 시 즉시 저장 거절 팝업 출력.

### 5. 못 정한 것
- 이미 완료된 과거 금지 품목 54건에 대한 정산/손해 발생 여부 $\rightarrow$ 감사팀 보고

---

# 마무리

## 검산

초기 파일 `shipments.csv`를 정제 자동화 스크립트(`process_shipments_re.py`)로 정제하여 생성한 작업 후 파일 `shipments_re.csv`를 감사팀 확인 정답 세트 `verified_300.csv` (299건)와 대조 검증을 완료하였습니다.

- **`accepted_at` (접수일시)**: 94.6% $\rightarrow$ **100.0%** (16건 향상)
- **`receiver_area` (도착지역)**: 92.3% $\rightarrow$ **100.0%** (23건 향상)
- **`eta_date` (도착예정일)**: 95.3% $\rightarrow$ **100.0%** (14건 향상)
- **`width_cm` (가로치수)**: 97.7% $\rightarrow$ **100.0%** (7건 향상)
- **`region_type` (권역구분)**: 99.3% $\rightarrow$ **100.0%** (2건 향상)
- **`price` (정산요금)**: 90.3% $\rightarrow$ **98.3%** (24건 향상)
- **`size_grade` (크기등급)**: 95.7% $\rightarrow$ **98.3%** (8건 향상)
- **`volume_weight_kg` (부피무게)**: 96.3% $\rightarrow$ **99.0%** (8건 향상)

검산 결과 11개 주요 항목에서 일치율이 상향되어 98%~100% 신뢰도를 확보하였습니다.

## 동료와 대 보기

경영진 핵심 질의 통계를 집계하여 사업 운영팀과 분석 결과를 비교 대조하였습니다.

- **지점별 미배송률**: 거제지점이 24.55% (55건/224건)로 타 지점(2.5%~4.8%) 대비 압도적으로 높음 $\rightarrow$ **팀 전원 일치 관찰**
- **지점별 배송 소요일**: 거제지점 평균 4.11일 (최대 13.27일) 소요 $\rightarrow$ **팀 전원 일치 관찰**
- **요금 오산정 누수 금액**: 순 1,246,500원 손실 발생 $\rightarrow$ **팀 전원 일치 관찰**

## 원본 보존

- [x] 원래 파일(`data/shipments.csv`)은 초기 로데이터로 그대로 유지하고, 정제 작업 후 새 파일(`data/shipments_re.csv`)로 생성함

원래 파일:
- `data/shipments.csv` (초기 파일)

새로 만든 파일:
- `data/shipments_re.csv` (작업 후 정제 파일)

## 이번에 다루지 않기로 한 것

1. 레거시 중복 운송장 45건의 원본 신규 번호 재발급 (이력 보존을 위해 `-DUP` 서픽스 태깅으로 처리)
2. 수취인 전화번호 미입력 265건의 삭제 처리 (고객 확인 전 임의 삭제 방지를 위해 `000-0000-0000` 태깅으로 처리)
3. 이미 완료된 레거시 금지 품목 54건 직권 취소 (이력 보존을 위해 `[금지품목]` 태깅으로 처리)

## 다음 사람에게

이 폴더의 정제 작업을 이어서 진행할 경우 아래 스크립트와 문서를 순서대로 확인하십시오.

1. **초기 파일**: `data/shipments.csv`
2. **작업 후 결과 파일**: `data/shipments_re.csv`
3. **정제 자동화 스크립트**: `python process_shipments_re.py` (실행 시 `shipments.csv`를 읽어 `shipments_re.csv` 재생성)
4. **검증 대조 스크립트**: `python compare_re_verified.py` (정답 세트 대조 결과 확인)
5. **경영 통계 분석 스크립트**: `python generate_business_metrics.py` (미배송률, 소요일, 요금차액 산출)
6. **개발 지침서**: `CLAUDE.md` (내일 UI 개발 규칙)

## 막힌 자리

없음

---

## 내일 미리 보기

내일은 정제된 데이터 처리 정책을 바탕으로 **접수 키오스크 웹 UI 화면**을 개발합니다.

* **기계적 차단**:
  * 도착지역 18개 광역시/도 드롭다운 선택 필드
  * 무게, 가로, 세로, 높이 입력 폼 (숫자 전용)
  * 세변합 > 160cm 또는 무게 > 25kg 초과 시 저장 버튼 비활성화
* **정책**:
  * 입력 치수/무게 기준 실시간 부피무게 및 요금 계산 자동 적용
  * 금지 품목 키워드 입력 시 접수 차단 팝업 출력
